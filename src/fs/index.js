export const cat = (dir, args) => {
  // cat path_to_file
}

export const add = (dir, args) => {
  // add new_file_name
}

export const rn = (dir, args) => {
  // rn path_to_file new_filename
}

export const cp = (dir, args) => {
  // cp path_to_file path_to_new_directory
}

export const mv = (dir, args) => {
  // mv path_to_file path_to_new_directory
}

export const rm = (dir, args) => {
  // rm path_to_file
}
